import { DraughtsComputerStrategyArgs, SearchEvaluationFunction } from './computer';
import { Bitboard, DraughtsEngineMove } from '../core/engine';
export type AlphaBetaOptions<T extends Bitboard, E> = {
    maxDepth: number;
    evaluationFunction: SearchEvaluationFunction<T, E>;
    quiescence?: boolean;
};
export declare function alphaBeta<T extends Bitboard, E>({ options: { maxDepth, evaluationFunction, quiescence }, engine, }: DraughtsComputerStrategyArgs<T, E, AlphaBetaOptions<T, E>>): Promise<DraughtsEngineMove<T>>;
